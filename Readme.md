## Test project for Rag pipeline using GROQ as base

# Intializing and installing necessary packages
npx create-next-app@latest docmind --typescript --tailwind --eslint --app --src-dir --import-alias "@/*"

# Set your own api key with creating ".env.local" file at root
GROQ_API_KEY=your_groq_api_key_here